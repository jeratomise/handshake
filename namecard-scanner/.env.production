# Build-time configuration for the deployed app.
#
# Both values are public by design: the URL is a hostname, and the publishable
# key is meant to ship in the client bundle. What protects the data is row
# level security on every table, not the secrecy of this key. The service_role
# key must never appear here or anywhere else in this repo.
#
# Vercel dashboard environment variables, if set, take precedence over this file.
VITE_SUPABASE_URL=https://oltrwsrzhmhmofwvyeju.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_dISbc0ePG3JkKXd1EliWMA_dZ2ZaxR2
